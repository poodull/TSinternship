#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../loadtest/bin/loadtest.js" "$@"
  ret=$?
else 
  node  "$basedir/../loadtest/bin/loadtest.js" "$@"
  ret=$?
fi
exit $ret
